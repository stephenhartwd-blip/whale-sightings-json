name: Update Whale Sightings

on:
  workflow_dispatch:
  schedule:
    # Runs every 6 hours (cron is interpreted in UTC by GitHub)
    - cron: "17 */6 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip show global-land-mask
          python -c "from global_land_mask import globe; print('global-land-mask OK')"

      - name: Run updater
        env:
          TZ: America/Vancouver
        run: |
          python scripts/update_whale_sightings.py

      - name: Commit & push if changed
        run: |
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A
          git commit -m "Auto-update whale sightings"
          git push
